FROM node:20

WORKDIR /app

# Copy package files and install dependencies
COPY package.json package-lock.json ./

# Clear npm cache and install with ARM64 compatibility
RUN npm install

# This base image contains all dependencies for the monorepo 